# API pública consumida por el frontend.
NEXT_PUBLIC_API_URL=

# Token server-only para POST /api/revalidate.
# Usar un valor aleatorio de al menos 32 caracteres.
REVALIDATION_TOKEN=

# Analizador opcional del bundle.
ANALYZE=false
