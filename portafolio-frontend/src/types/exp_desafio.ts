export type ExpDesafioItem = {
    id: number;
    exp_id: number;
    exp_desafio: string;
    exp_solucion: string;
};

// Exportamos un array de ese item
export type exp_desafioType = ExpDesafioItem[];